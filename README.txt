Title:
Description: There is a web page with a simple JavaScript and css file linked to the page. The page also uses CSS and JavaScript from the Bootstrap library (the page doesn't reguire the JS file but it adds another layer of files to look through). The key is hidden in one of these files.
Solution: The key is labeled "guess" in a css comment 4/5 down the css document. 
Key: mca-efefefff